name = "nibbles"
compatibility_date = "2026-03-04"
pages_build_output_dir = "."

# KV bindings for Cloudflare Pages are typically configured in your Pages project
# settings (Settings -> Functions -> KV namespace bindings).
#
# For local dev without hardcoding KV IDs here, you can run:
#   wrangler pages dev . --kv NIBBLES_ROOMS
